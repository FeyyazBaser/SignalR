@{
    ViewData["Title"] = "Home Page";
}

@section Scripts
    {
    <script src="~/lib/microsoft/signalr/dist/browser/signalr.min.js"></script>

    <script type="text/javascript">

        var connection = new signalR.HubConnectionBuilder().withUrl("https://localhost:7026/MyHub")

        $("#consStatus").text(connection.connectionState);

        $("#btnNameSave").click(() => {
            connection.invoke("SendName", $("txtName").val()).catch((err) => console.log(err))
        })

        connection.start().then(() => {
            $("#consStatus").text(connection.connectionState);
        }).catch((err) => { console.log(err) });

    </script>
}
<div class="row">

    <div class="col-md-8 offset-2">
        <input class="form-control" type="text" id="txtName" />
        <hr />
        <button class="btn btn-primary" id="btnNameSave">İsim Kaydet</button>
        <div class="alert alert-info">
            Bağlantı durumu:<strong id="conStatus"></strong>

        </div>
    </div>
</div>
